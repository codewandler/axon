# Improvement Report — 2026-04-10

Generated by `/improve`. Focus: `README.md`. Each item is independently actionable.

Methodology: every finding was confirmed by reading `README.md` against actual source files
(`types/markdown.go`, `types/golang.go`, `types/vcs.go`, `types/project.go`, `types/edges.go`,
`indexer/` directory listing, `axon.go`, and all `cmd/axon/*.go` command files).

---

## Auto (spawning sub-agents now)

Items in this section are low-risk, self-contained, and will be applied immediately by background sub-agents.

---

### AUTO-1: Remove fictitious `md:heading` node type from Node Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: Line 734 lists `- \`md:heading\` - Heading` as a node type. This type does **not exist** in the codebase. `types/markdown.go` defines `md:document`, `md:section`, `md:codeblock`, `md:link`, `md:image`. There is no `md:heading` constant or node creation. The `md:section` type already represents heading-based sections (the `SectionData.Level` field carries the H1–H6 level). The entry is stale — it appears to be a renamed type from an earlier iteration.
- **Fix**: Delete the line `- \`md:heading\` - Heading` from the Documents subsection of Node Types (README.md line 734). No other changes needed.

---

### AUTO-2: Add missing `md:codeblock`, `md:link`, `md:image` node types to Node Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Documents subsection of Node Types (lines 730–734) lists only `md:document` and `md:section` (and the wrong `md:heading`). Three real types are missing: `md:codeblock` (fenced code blocks), `md:link` (external links), `md:image` (image references). All three are defined in `types/markdown.go` and actively emitted by `indexer/markdown/indexer.go`.
- **Fix**: After the fix in AUTO-1, the Documents subsection should read:
  ```
  ### Documents

  - `md:document` - Markdown document
  - `md:section` - Document section (heading-based; `data.level` = 1–6)
  - `md:codeblock` - Fenced code block
  - `md:link` - External link
  - `md:image` - Image reference
  ```
  Replace the existing Documents block (lines 730–734) with the above.

---

### AUTO-3: Add missing `go:module` node type to Node Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Go Code subsection of Node Types (lines 736–743) lists `go:package`, `go:func`, `go:struct`, `go:interface`, `go:ref`, but is missing `go:module`. The constant `TypeGoModule = "go:module"` is defined in `types/golang.go` line 7 and is emitted by `indexer/golang/indexer.go` as the root of every Go module.
- **Fix**: Add `- \`go:module\` - Go module (go.mod root)` as the first item in the Go Code subsection, before `go:package`.

---

### AUTO-4: Add missing `vcs:remote` node type to Node Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Version Control subsection (lines 726–729) lists `vcs:repo`, `vcs:branch`, `vcs:tag`, `vcs:commit` but omits `vcs:remote`. The constant `TypeRemote = "vcs:remote"` is defined in `types/vcs.go` line 12 and is actively emitted by `indexer/git/indexer.go` when it discovers Git remotes.
- **Fix**: Add `- \`vcs:remote\` - Git remote` after `vcs:repo` in the Version Control subsection.

---

### AUTO-5: Add missing `project:root` node type to Node Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: A `project:root` node type exists (`TypeProject = "project:root"` in `types/project.go`) and is actively emitted by `indexer/project/indexer.go` when it detects project manifests (go.mod, package.json, Cargo.toml, pyproject.toml, pom.xml, etc.). This entire category is absent from the Node Types section of README.md.
- **Fix**: Add a new **Project** subsection after the Go Code subsection in Node Types:
  ```
  ### Project

  - `project:root` - Project root detected from a manifest file (go.mod, package.json, Cargo.toml, etc.)
    - Data: `type` (language: go, node, rust, python, java, ruby, php), `name`, `version`, `dep_count`
  ```

---

### AUTO-6: Add missing `parent_of` and `modifies` edge types to Edge Types section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Edge Types section (lines 748–758) lists 10 edge types but is missing two that are defined in `types/edges.go` and actively emitted by `indexer/git/indexer.go`:
  - `parent_of` (`EdgeParentOf = "parent_of"`) — Commit DAG parent-to-child relationship
  - `modifies` (`EdgeModifies = "modifies"`) — Commit modified a file
- **Fix**: Append two entries to the Edge Types bullet list:
  ```
  - `parent_of` - Commit DAG parent-to-child (vcs:commit → vcs:commit)
  - `modifies` - Commit modified a file (vcs:commit → fs:file)
  ```

---

### AUTO-7: Add `golang` indexer to Architecture section

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Architecture section (lines 767–772) lists the pluggable indexers as `fs`, `git`, `markdown` — but `indexer/golang/` (the Go source code indexer that creates `go:*` nodes) is absent from this list. The Go indexer is real and actively used — it's what populates the `go:func`, `go:struct`, etc. nodes that the README documents in Node Types.
- **Fix**: Add `- \`golang\` - Go source code indexer` to the indexer list in the Architecture section, after `markdown`:
  ```
  - **Indexers** (`indexer/`) - Pluggable indexers for different data sources
    - `fs` - Filesystem indexer
    - `git` - Git repository indexer
    - `markdown` - Markdown document indexer
    - `golang` - Go source code indexer
    - `project` - Project manifest indexer (go.mod, package.json, …)
  ```

---

## Needs Your Input

### HUMAN-1: Expand Architecture section — Context engine and tagger

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Architecture section omits two components that are real and significant:
  1. **`context/` package** — a standalone context-generation engine used by `axon context`. It has its own sub-packages (`budget`, `task`, `walker`, `source`, `format`).
  2. **`indexer/tagger/`** — a label-tagging indexer that applies labels to nodes based on name/path patterns (e.g., CI files, dotfiles).
  These are non-trivial pieces of the system but add architectural detail beyond what most users need.
- **Proposed change**: Add two bullets to the Architecture section, plus a note about the `context/` package.
- **Why it needs input**: Judgment call on how much architectural depth to expose. The current Architecture section is intentionally brief. Adding `tagger` may also require explaining how it works (rule-based labeling), which could grow the section significantly.

### HUMAN-2: Expand Go Code node types — `go:method`, `go:field`, `go:const`, `go:var`

- **Category**: docs
- **Risk**: low
- **Files**: `README.md`
- **Finding**: The Go Code subsection lists `go:package`, `go:func`, `go:struct`, `go:interface`, `go:ref`. The following types also exist in `types/golang.go` and are emitted by the Go indexer:
  - `go:method` (`TypeGoMethod`) — method nodes
  - `go:field` (`TypeGoField`) — struct fields
  - `go:const` (`TypeGoConst`) — constants
  - `go:var` (`TypeGoVar`) — package-level variables
  These are useful for fine-grained queries but significantly expand the node types table.
- **Proposed change**: Add these four types to the Go Code subsection with brief descriptions.
- **Why it needs input**: The current list intentionally shows the most commonly queried types. Adding all subtypes may make the table overwhelming. You may prefer a note like "see `types/golang.go` for the full type list" instead.

---

## Skipped / Out of Scope

- **`fs:link` node type** — Defined in `types/fs.go` (`TypeLink = "fs:link"`) but not verified to be emitted by the fs indexer in common usage (symlinks). Skipped to avoid adding a type that may not appear in most graphs.
- **`vcs:remote` in "What gets indexed" description** — The `axon index` description at line 310–313 mentions the correct categories but omits remotes. Folded into AUTO-4 implicitly — fixing Node Types is the more visible doc fix.
- **AQL syntax accuracy** — The AQL examples in the README were spot-checked against the parser source; no discrepancies found.
- **Go Library code examples** — Cross-checked `Querier` interface, `WriteNode`, `PutNode`, `Flush`, `GetNodeByURI`, `Search`, `Find`, `QueryString` against `axon.go`; all match.
- **`md:heading` in `aql/nodetypes.go`** — The AQL fluent builder's `NodeType.Heading` field references `"md:heading"`. This is a code issue (stale AQL constant), not a README issue, and is out of scope for this README-focused pass.
