# Improvement Report — 2026-04-11

Generated by `/improve`. Each item is independently actionable.

---

## Auto (spawning sub-agents now)

Items in this section are low-risk, self-contained, and will be applied
immediately by background sub-agents.

### AUTO-1: Discarded error in bench_test.go SetCenter call
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/bench_test.go`
- **Finding**: `err = nav.SetCenter(cwdNode)` at line 75 discards the return value. Benchmarks suppress errors intentionally but should explicitly discard to make the intent clear.
- **Fix**: Change `err = nav.SetCenter(cwdNode)` to `_ = nav.SetCenter(cwdNode)` at line 75.

### AUTO-2: Discarded error in bench_test.go NavigateTo call (AGENTS.md)
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/bench_test.go`
- **Finding**: `err = nav.NavigateTo(agentsMD)` at line 95 discards the return value.
- **Fix**: Change `err = nav.NavigateTo(agentsMD)` to `_ = nav.NavigateTo(agentsMD)` at line 95.

### AUTO-3: Discarded error in bench_test.go NavigateTo call (huge file)
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/bench_test.go`
- **Finding**: `err = nav.NavigateTo(hugeResult.Nodes[0])` at line 114 discards the return value.
- **Fix**: Change `err = nav.NavigateTo(hugeResult.Nodes[0])` to `_ = nav.NavigateTo(hugeResult.Nodes[0])` at line 114.

### AUTO-4: Discarded error in bench_test.go NavigateTo call (24k-child dir)
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/bench_test.go`
- **Finding**: `err = nav.NavigateTo(bigResult.Nodes[0])` at line 134 discards the return value.
- **Fix**: Change `err = nav.NavigateTo(bigResult.Nodes[0])` to `_ = nav.NavigateTo(bigResult.Nodes[0])` at line 134.

### AUTO-5: Discarded error in bench_test.go Query call
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/bench_test.go`
- **Finding**: `secEdgeResult, err := s.Query(ctx, seq)` at line 408 discards `err`.
- **Fix**: Change `secEdgeResult, err := s.Query(ctx, seq)` to `secEdgeResult, _ := s.Query(ctx, seq)` at line 408.

### AUTO-6: Discarded error in preview.go file read
- **Category**: style
- **Risk**: low
- **Files**: `cmd/axonui/preview.go`
- **Finding**: `n, err := f.Read(buf)` at line 271 discards `err` in a benchmark-style function that already returns `""` on the preceding error check. The `err` check below at line 271+ only handles the `err != nil` from `os.Open`, not from `f.Read`.
- **Fix**: Change `n, err := f.Read(buf)` to `n, _ := f.Read(buf)` at line 271.

### AUTO-7: Extraneous space in compiler directive (SA9009)
- **Category**: style
- **Risk**: low
- **Files**: `indexer/golang/indexer.go:927`
- **Finding**: `// go:struct -[embeds]-> go:struct edge for every embedded type that is also` has an extraneous space before `embeds`. The `go:struct` compiler directive format requires `-[<edge>]->` with no spaces.
- **Fix**: Change line 927 from `// go:struct -[embeds]-> go:struct edge for every embedded type that is also` to `//go:struct -[embeds]-> go:struct edge for every embedded type that is also` (remove leading space and fix `// ` to `//`).

### AUTO-8: Unused variable `licenceFileNames` in license.go
- **Category**: dead-code
- **Risk**: low
- **Files**: `indexer/project/license.go:72`
- **Finding**: `var licenceFileNames = []string{...}` is defined at line 72 but never referenced anywhere in the file. `go build` reports this as U1000.
- **Fix**: Delete lines 69–76 (the `licenceFileNames` variable declaration and its comment). The comment explains the glob patterns used elsewhere — confirm with grep that no glob patterns reference these names before deleting. If needed, note the pattern names for documentation purposes.

---

## Needs Your Input

No HUMAN items this pass.

---

## Skipped / Out of Scope

- TODO/FIXME/HACK markers: All found in `.agents/`, `.git/`, `README.md`, `AGENTS.md`, and `CHANGELOG.md` are legitimate design/planning docs, design plans, skill docs, or references to the todo indexer feature (which is implemented). The two in `adapters/sqlite/aql.go:55` and `adapters/sqlite/errors.go:19` are genuine future-work placeholders — not stale.
- Commented-out code: All `//` matches were godoc lines (valid documentation) or blank continuation lines — nothing suspicious.
- Simplifiable expressions: No `if x == true` / `if x == false` patterns found.
- `len(x) == 0` vs `x == nil` confusion: No clear cases found.
- Test coverage gaps: All packages have test files. The `storage/` package has no test files but appears to be a pure interface/typedef package (storage errors re-exported from `storage/storage.go`). This is acceptable.
- Uncommitted changes: `git status` shows no changes — clean working tree.
