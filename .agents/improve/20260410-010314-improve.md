# Improvement Report — 2026-04-10

Generated by `/improve`. Each item is independently actionable.

---

## Auto (spawning sub-agents now)

Items in this section are low-risk, self-contained, and will be applied
immediately by background sub-agents.

### AUTO-1: Remove unused `formatTimestamp` function
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axon/info.go`
- **Finding**: `func formatTimestamp(t time.Time) string` at line 233 is defined but never called. Confirmed by staticcheck U1000.
- **Fix**: Delete lines 232–234 in `cmd/axon/info.go` (the doc comment + function body). Also remove the `time` import if it becomes unused after the deletion.

### AUTO-2: Remove unused `fileExtension` function
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axontui/preview.go`
- **Finding**: `func fileExtension(node *graph.Node) string` at line 465 is defined but never called. Confirmed by staticcheck U1000.
- **Fix**: Delete the function and its doc comment (lines 464–480 approx). Check if any imports become unused.

### AUTO-3: Remove unused `edgeArrow` function
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axontui/util.go`
- **Finding**: `func edgeArrow(incoming bool) string` at line 47 is defined but never called. Confirmed by staticcheck U1000.
- **Fix**: Delete lines 46–53 (doc comment + function). Check imports.

### AUTO-4: Remove unused `padRight` function
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axontui/util.go`
- **Finding**: `func padRight(s string, width int) string` at line 66 is defined but never called. Confirmed by staticcheck U1000.
- **Fix**: Delete lines 65–71 (doc comment + function). Check imports.

### AUTO-5: Remove unused style variables in `cmd/axontui/styles.go`
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axontui/styles.go`
- **Finding**: Five unused `lipgloss.Style` variables confirmed by staticcheck U1000:
  - `borderStyle` at line 36
  - `nodeTypeStyle` at line 50
  - `sectionCountStyle` at line 81
  - `breadcrumbStyle` at line 112
  - `breadcrumbSepStyle` at line 115
- **Fix**: Delete each of these variable declarations (and their preceding comment lines). They are inside a `var (...)` block; remove just those entries, leaving the rest intact. Verify the `var (...)` block still has other entries so it remains valid.

### AUTO-6: Remove unused `skipIndex` field from `discoveredEntry`
- **Category**: dead-code
- **Risk**: low
- **Files**: `indexer/fs/indexer.go`
- **Finding**: `skipIndex bool` field at line 60 of struct `discoveredEntry` is never read or written to (beyond its zero value). Confirmed by staticcheck U1000.
- **Fix**: Delete line 60 (`skipIndex bool // If true, skip indexing (out of bounds)`).

### AUTO-7: Remove unused `contentStart` / `contentEnd` fields from `sectionInfo`
- **Category**: dead-code
- **Risk**: low
- **Files**: `indexer/markdown/indexer.go`
- **Finding**: `contentStart int` and `contentEnd int` at lines 229–230 of struct `sectionInfo` are never read or written. Confirmed by staticcheck U1000.
- **Fix**: Delete lines 229–230 from the `sectionInfo` struct.

### AUTO-8: Remove unused `pomNameRegex` and `pomVersionRegex` variables
- **Category**: dead-code
- **Risk**: low
- **Files**: `indexer/project/indexer.go`
- **Finding**: `pomNameRegex` at line 381 and `pomVersionRegex` at line 382 are declared but never used. Confirmed by staticcheck U1000.
- **Fix**: Delete lines 380–382 (the comment + two `var` declarations). If `regexp` import becomes unused after deletion, remove it too.

### AUTO-9: Fix unnecessary `fmt.Sprintf` with no format verbs in `adapters/sqlite/aql.go`
- **Category**: simplify
- **Risk**: low
- **Files**: `adapters/sqlite/aql.go`
- **Finding**: Two calls at lines 1427 and 1429 wrap plain string literals in `fmt.Sprintf(...)` with no format verbs. Flagged by staticcheck S1039.
  - Line 1427: `sql.WriteString(fmt.Sprintf("SELECT n_end.*\nFROM edges e0\n"))`
  - Line 1429: `sql.WriteString(fmt.Sprintf("SELECT n_start.*\nFROM edges e0\n"))`
- **Fix**: Replace each with a plain `sql.WriteString("...")` call, removing the `fmt.Sprintf` wrapper.

### AUTO-10: Fix unnecessary `fmt.Sprintf` with no format verbs in `cmd/axon/search.go`
- **Category**: simplify
- **Risk**: low
- **Files**: `cmd/axon/search.go`
- **Finding**: Line 651: `structQuery, _ := aql.Parse(fmt.Sprintf(\`SELECT DISTINCT * FROM nodes WHERE type = 'go:struct' ORDER BY name LIMIT 50\`))` — `fmt.Sprintf` wraps a plain string literal. Flagged by staticcheck S1039.
- **Fix**: Replace with `structQuery, _ := aql.Parse(\`SELECT DISTINCT * FROM nodes WHERE type = 'go:struct' ORDER BY name LIMIT 50\`)`.

### AUTO-11: Replace `WriteString(fmt.Sprintf(...))` with `fmt.Fprintf(...)` in `adapters/sqlite/aql.go`
- **Category**: simplify
- **Risk**: low
- **Files**: `adapters/sqlite/aql.go`
- **Finding**: Three locations use `sql.WriteString(fmt.Sprintf(...))` when `fmt.Fprintf(&sql, ...)` would be more idiomatic. Flagged by staticcheck QF1012 at lines 482, 484, 487.
- **Fix**:
  - Line 482: `sql.WriteString(fmt.Sprintf(" WHERE path.depth + 1 >= %d", *varLenEdge.MinHops))` → `fmt.Fprintf(&sql, " WHERE path.depth + 1 >= %d", *varLenEdge.MinHops)`
  - Line 484: `sql.WriteString(fmt.Sprintf(" AND path.depth + 1 <= %d", *varLenEdge.MaxHops))` → `fmt.Fprintf(&sql, " AND path.depth + 1 <= %d", *varLenEdge.MaxHops)`
  - Line 487: `sql.WriteString(fmt.Sprintf(" WHERE path.depth + 1 <= %d", *varLenEdge.MaxHops))` → `fmt.Fprintf(&sql, " WHERE path.depth + 1 <= %d", *varLenEdge.MaxHops)`

### AUTO-12: Omit redundant type in variable declaration in `adapters/sqlite/aql.go`
- **Category**: style
- **Risk**: low
- **Files**: `adapters/sqlite/aql.go`
- **Finding**: Line 1341: `var varLenEdgeIdx int = -1` — the type `int` is inferred from the right-hand side and can be omitted. Flagged by staticcheck QF1011.
- **Fix**: Change to `var varLenEdgeIdx = -1`.

### AUTO-13: Simplify single-iteration loop to `if` in `indexer/markdown/indexer.go`
- **Category**: simplify
- **Risk**: low
- **Files**: `indexer/markdown/indexer.go`
- **Finding**: Lines 364–371 contain a `for` loop that unconditionally breaks after the first iteration (SA4004). The loop condition variable (`len(w.headingPositions)`) never changes (SA4008). The loop always executes at most once and is equivalent to an `if` guard.
- **Fix**: Replace:
  ```go
  for i := headingIdx + 1; i < len(w.headingPositions); i++ {
      nextPos := w.headingPositions[i]
      // Any heading (child or sibling) ends our content
      if nextPos.startPos < end {
          end = nextPos.startPos
      }
      break // Only need to check the next heading
  }
  ```
  With:
  ```go
  if headingIdx+1 < len(w.headingPositions) {
      nextPos := w.headingPositions[headingIdx+1]
      // Any heading (child or sibling) ends our content
      if nextPos.startPos < end {
          end = nextPos.startPos
      }
  }
  ```

### AUTO-14: Use type conversion instead of struct literal in `context/format.go`
- **Category**: style
- **Risk**: low
- **Files**: `context/format.go`
- **Finding**: Line 164 constructs a `JSONExclude` struct by copying all fields from an `ExcludedInfo` value, but the two types share the same field names and types — a type conversion is cleaner. Flagged by staticcheck S1016.
- **Fix**: Replace the struct literal with a type conversion. Find the `ExcludedInfo` definition to confirm field identity, then change:
  ```go
  output.Excluded = append(output.Excluded, JSONExclude{
      File:   ex.File,
      Tokens: ex.Tokens,
      Score:  ex.Score,
      Reason: ex.Reason,
  })
  ```
  to:
  ```go
  output.Excluded = append(output.Excluded, JSONExclude(ex))
  ```
  Only apply if `ExcludedInfo` and `JSONExclude` have identical fields (verify first).

### AUTO-15: Replace deprecated `strings.Title` with `cases.Title` in `cmd/axon/query.go`
- **Category**: dead-code
- **Risk**: low
- **Files**: `cmd/axon/query.go`
- **Finding**: `strings.Title` is deprecated since Go 1.18 (SA1019) at lines 396 and 545. `golang.org/x/text v0.31.0` is already a dependency in `go.mod`.
- **Fix**:
  1. Add import `"golang.org/x/text/cases"` and `"golang.org/x/text/language"` (if not already present).
  2. Replace both occurrences:
     - `strings.Title(strings.ReplaceAll(strings.ReplaceAll(col, "_", " "), ".", " "))` → `cases.Title(language.English).String(strings.ReplaceAll(strings.ReplaceAll(col, "_", " "), ".", " "))`
  3. Remove the `strings` import if it becomes unused (it likely won't since `strings.ReplaceAll` is still used).

### AUTO-16: Remove unused `compileCTEVariableLength` method
- **Category**: dead-code
- **Risk**: low
- **Files**: `adapters/sqlite/aql.go`
- **Finding**: `func (s *Storage) compileCTEVariableLength(...)` at lines 1500–1644 (doc comment at 1501, function at 1504) is defined but never called. Confirmed by staticcheck U1000. It is a ~140-line method that appears to be superseded by the current variable-length path compilation approach.
- **Fix**: Delete lines 1500–1644 inclusive (doc comment + entire function body). Verify no imports become dangling.

---

## Needs Your Input

### HUMAN-1: Empty `if` branch in `cmd/axontui/pane.go` suggests missing feature
- **Category**: behaviour
- **Risk**: medium
- **Files**: `cmd/axontui/pane.go`
- **Finding**: Lines 61–63 contain an `if g.Loaded && len(g.Nodes) < g.Count { // We'll show "... +N more" inline }` — the body is empty. The comment indicates a "show more" indicator was planned but never implemented. Staticcheck flags this as SA9003 (empty branch).
- **Proposed change**: Either implement the "show more" indicator, or remove the empty `if` block if the feature is dropped.
- **Why it needs input**: Uncertain whether this is planned work or a feature that was abandoned. Removing it silently discards the intent.

### HUMAN-2: Tagged switch improvement in `indexer/project/indexer_test.go`
- **Category**: style
- **Risk**: low
- **Files**: `indexer/project/indexer_test.go`
- **Finding**: Line 604 uses `if/else if` on `sub.EventType` where a `switch` would be cleaner. Flagged by staticcheck QF1003.
- **Proposed change**: Convert the `if/else if` chain on `sub.EventType` to a `switch sub.EventType { case ... }`.
- **Why it needs input**: It's a test file; the change is purely cosmetic and very low risk, but it's a test so I want to flag it rather than silently auto-modify test logic.

---

## Skipped / Out of Scope

- **`defer rows.Close()` unchecked errors** (47 errcheck findings): Deferring `Close()` without checking the error return is an accepted Go idiom for cleanup paths. Wrapping all of these adds noise for negligible safety benefit.
- **`cmd/axontui/bench_test.go` ineffectual assignments** (lines 75, 95): Test scaffolding; low value to fix.
- **`cmd/axontui/preview.go:271` ineffectual assignment** (`n, err := f.Read(buf)`): `n` is actually used (checked for `n == 0`), but `err` after the read is not used. Low risk, test-adjacent.
- **TODOs in `adapters/sqlite/aql.go` and `adapters/sqlite/errors.go`**: Legitimate future work (query cache, better error messages) — not stale.
- **TODO in `cmd/axon/search.go`**: Notes that proper type checking requires go/types analysis — accurate and non-stale.
