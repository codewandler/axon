# Improvement Report — 2026-04-10

Generated by `/improve`. Focus: axon skill files and AGENTS.md — token count / size.

Methodology: every finding was confirmed by reading the actual skill files
(`.agents/skills/axon/SKILL.md`, `references/aql.md`,
`references/aql_go_querybuilder.md`) and cross-checking against `AGENTS.md`
and the live codebase (`types/*.go`, `aql/builder.go`).

---

## Auto (spawning sub-agents now)

### AUTO-1: Fix stale `md:heading` type and add missing markdown node types in SKILL.md

- **Category**: docs
- **Risk**: low
- **Files**: `.agents/skills/axon/SKILL.md`
- **Finding**: Line 113 lists `md:heading` which does not exist in the codebase
  (`types/markdown.go` defines `md:document`, `md:section`, `md:codeblock`,
  `md:link`, `md:image`). Three real types are also missing from the list.
- **Fix**: Replace the Documents line:
  ```
  - `md:document`, `md:section`, `md:heading` - Markdown
  ```
  with:
  ```
  - `md:document`, `md:section` - Markdown document/section
  - `md:codeblock`, `md:link`, `md:image` - Fenced code, links, images
  ```

---

### AUTO-2: Add missing node types to SKILL.md

- **Category**: docs
- **Risk**: low
- **Files**: `.agents/skills/axon/SKILL.md`
- **Finding**: Several real node types are absent from the Node Types section
  (confirmed in `types/*.go` and README.md):
  - `vcs:remote` — defined in `types/vcs.go`, emitted by `indexer/git`
  - `project:root` — defined in `types/project.go`, emitted by `indexer/project`
  - `go:module` — defined in `types/golang.go`, emitted by `indexer/golang`
  - `go:method`, `go:field`, `go:const`, `go:var` — all in `types/golang.go`
- **Fix**: After the existing Node Types list, add:
  - To the VCS line: append `, vcs:remote` (making it `vcs:repo`, `vcs:branch`, `vcs:tag`, `vcs:commit`, `vcs:remote`)
  - To the Go line: expand to `go:module`, `go:package`, `go:func`, `go:method`, `go:struct`, `go:field`, `go:interface`, `go:const`, `go:var`, `go:ref`
  - Add a new line: `- \`project:root\` - Project manifest (go.mod, package.json, Cargo.toml, …)`

---

### AUTO-3: Add missing edge types to SKILL.md

- **Category**: docs
- **Risk**: low
- **Files**: `.agents/skills/axon/SKILL.md`
- **Finding**: Four edge types defined in `types/edges.go` and used in the
  codebase are absent from the Edge Types section:
  - `depends_on` — dependency relationship
  - `tests` — test package → source package
  - `parent_of` — commit DAG
  - `modifies` — commit → file
- **Fix**: Append to the Edge Types bullet list:
  ```
  - `depends_on` - Dependency relationship
  - `tests` - Test package tests source package
  - `parent_of` - Commit DAG parent-to-child
  - `modifies` - Commit modified a file
  ```

---

### AUTO-4: Remove Table of Contents from `aql_go_querybuilder.md`

- **Category**: style (token savings)
- **Risk**: low
- **Files**: `.agents/skills/axon/references/aql_go_querybuilder.md`
- **Finding**: Lines 5–17 contain a 13-line Table of Contents with markdown
  anchor links. AI agents reading this as context do not navigate via anchor
  links; the ToC is pure overhead (~120 tokens).
- **Fix**: Delete lines 5–17 (the ToC block and the `---` separator on line 17).

---

### AUTO-5: Remove duplicate `Between` line in `aql_go_querybuilder.md`

- **Category**: style
- **Risk**: low
- **Files**: `.agents/skills/axon/references/aql_go_querybuilder.md`
- **Finding**: `aql.DataSize.Between(100, 1000)` appears twice in the
  Comparisons block — once at line 139 (with `// data.size BETWEEN 100 AND 1000`)
  and again at line 148 (identical, listed under "Set operations" where it
  doesn't belong).
- **Fix**: Delete the second occurrence (line 148):
  `aql.DataSize.Between(100, 1000)     // data.size BETWEEN 100 AND 1000`

---

### AUTO-6: Remove "Benefits of New API" section from `aql_go_querybuilder.md`

- **Category**: style (token savings)
- **Risk**: low
- **Files**: `.agents/skills/axon/references/aql_go_querybuilder.md`
- **Finding**: Lines 602–611 contain a "Benefits of New API" bullet list
  (type safety, auto-completion, cleaner code, etc.). This is marketing copy
  appended to a technical reference document; it adds no actionable information
  for an agent building queries (~80 tokens).
- **Fix**: Delete lines 602–611 (the "Benefits of New API" bullet list and
  the final sentence about AST compatibility).

---

### AUTO-7: Remove Migration Guide section from `aql_go_querybuilder.md`

- **Category**: docs (token savings ~1,300 tokens)
- **Risk**: low
- **Files**: `.agents/skills/axon/references/aql_go_querybuilder.md`
- **Finding**: Lines 464–601 are a 138-line "Migration Guide" that documents
  the OLD string-based AQL API (`SelectStar().From("nodes")`,
  `Eq("type", String("fs:file"))`, etc.) and how to port it to the new fluent
  API. The old string API is gone from the codebase. No file in the repository
  uses it. An agent writing new code today never needs this guide; it just
  consumes tokens without benefit.
- **Fix**: Delete lines 464–601 in full (the `## Migration Guide` heading and
  all content through end of file). The file will end after the Variable
  References section.

---

### AUTO-8: Remove "Common CLI Patterns" section from `aql_go_querybuilder.md`

- **Category**: docs (token savings ~630 tokens)
- **Risk**: low
- **Files**: `.agents/skills/axon/references/aql_go_querybuilder.md`
- **Finding**: Lines 392–461 (70 lines) titled "Common CLI Patterns" contain
  three sub-sections:
  - "Find Commands" — identical to Table Queries examples already in the file
  - "Statistics Commands" — identical to Aggregation Queries examples
  - "Scoped Statistics" — identical to Scoped Queries examples
  Every example in this section already appears in an earlier section of the
  same file. It is pure duplication with a different heading.
- **Fix**: Delete lines 392–461 in full (the `## Common CLI Patterns` heading
  and all content through `## Migration Guide`). The Scoped Queries section
  will flow directly into what was the Migration Guide (which AUTO-7 also
  deletes, so the file ends after Variable References).

---

### AUTO-9: Remove "Removed: GraphTraverser" section from `AGENTS.md`

- **Category**: docs (token savings ~210 tokens)
- **Risk**: low
- **Files**: `AGENTS.md`
- **Finding**: Lines 408–431 document the `Traverse` method and
  `GraphTraverser` interface that were removed from the codebase. The section
  describes a feature that no longer exists, and the "migration" example at the
  bottom still uses the old string-based AQL API (which was also removed — see
  AUTO-7). This section has no value to a developer working on the current
  codebase.
- **Fix**: Delete lines 408–431 in full (from `### Removed: GraphTraverser`
  through the closing ` ``` ` of the migration code block). The `### CLI
  Commands` section at line 432 will follow directly after `### AQL`.

---

## Needs Your Input

### HUMAN-1: Condense "New: Type-Safe Fluent AQL API" section in `AGENTS.md` (~166 lines → ~10 lines)

- **Category**: docs (token savings ~1,400 tokens)
- **Risk**: medium
- **Files**: `AGENTS.md`
- **Finding**: Lines 242–407 (166 lines, ~4,300 chars) are the "New:
  Type-Safe Fluent AQL API" section. It contains full examples for every API
  construct: Key Constants, Expression Methods, Variable References, Migration
  table, and a "CLI Commands Using New API" sub-section. This exact content
  lives in `.agents/skills/axon/references/aql_go_querybuilder.md` which is a
  dedicated, loadable skill reference. AGENTS.md gets injected into every agent
  session on this repository, even sessions not touching AQL at all. The 166
  lines cost tokens on every single invocation.
- **Proposed change**: Replace the entire "New: Type-Safe Fluent AQL API"
  section (lines 242–407) with a brief pointer:
  ```markdown
  ### AQL Builder API
  
  Use the type-safe fluent builder for all programmatic AQL construction.
  Full reference: `.agents/skills/axon/references/aql_go_querybuilder.md`
  
  Quick examples:
  ```go
  aql.Nodes.Select(aql.Type, aql.Count()).GroupBy(aql.Type).Build()
  aql.Nodes.JsonEach(aql.Labels).Select(aql.Val, aql.Count()).GroupBy(aql.Val).Build()
  aql.Nodes.SelectStar().Where(aql.Nodes.ScopedTo(cwdNodeID)).Build()
  ```
  ```
- **Why it needs input**: Removes significant detail from AGENTS.md. Agents
  that need the full API reference must load the skill explicitly. If you
  frequently have agents work on AQL-related code, keeping some examples inline
  may be preferable.

---

### HUMAN-2: Condense AQL Phase breakdown in `AGENTS.md` to a capability summary (~50 lines → ~10 lines)

- **Category**: docs (token savings ~350 tokens)
- **Risk**: low
- **Files**: `AGENTS.md`
- **Finding**: Lines 192–241 describe AQL capabilities using a "Phase 1/2/3/4"
  roadmap format with "✅ implemented" badges. Since all four phases are
  complete, the phase framing adds no information — it's a project-plan artifact
  that survived past its useful life. The same capabilities can be described in
  ~10 lines.
- **Proposed change**: Replace lines 196–228 (the Phase 1–4 blocks and
  Performance Optimizations) with:
  ```markdown
  AQL supports: table queries (SELECT/WHERE/GROUP BY/HAVING/ORDER BY/LIMIT),
  JSON field access (`data.ext`), label operations (`CONTAINS ANY/ALL`),
  pattern matching (`(var:type)-[:edge]->(var:type)`), variable-length paths
  (`[:contains*1..3]`), table functions (`json_each`), and EXISTS scoped queries.
  Scoped queries use a CTE+JOIN rewrite for performance.
  ```
- **Why it needs input**: Minor restructuring of developer-facing content.
  The phase labels may be meaningful in your team's mental model.

---

## Skipped / Out of Scope

- **`aql.md` "Common Patterns" section** (lines 237–273): Two of the six
  examples (`imports`, `references`) are not duplicated elsewhere in the file,
  so the section is not pure waste. Left as-is.
- **`aql.md` "Query Modifiers" section**: Mostly covered in earlier examples
  but provides a concise summary for quick reference. Kept.
- **`cmd/axontui/bench_test.go` SA4006 findings**: Six unused `err` values in
  test benchmarks — already noted in previous improve pass as "test scaffolding,
  low value to fix." Skipping again.
- **`cmd/axontui/preview.go:271` SA4006**: Previously noted and skipped.
